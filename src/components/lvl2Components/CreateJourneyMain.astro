---
// src/components/CreativeJourney.astro
import { Image } from "astro:assets";
import cryptocademyImg from "../../assets/app-showcase/Cryptocademy-showcase.png";
import GestureAcademyImg from "../../assets/app-showcase/GestureAcademy-showcase.png";
import Workoutwiki from "../../assets/app-showcase/Workoutwiki-showcase.png";
import menyouai from "../../assets/app-showcase/me-n-you-teaser.png";
---

<style>
  /*
    This style block contains the more complex CSS for the hover animations,
    including the animated gradient border effect for the project cards.
  */
  .project-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    border-radius: 0.5rem; /* Matches the card's rounded corners */
    border: 2px solid transparent;
    /* Changed to your new linear-gradient */
    background: linear-gradient(360deg, #1a2a4c, #5676c9d1, white) border-box;
    -webkit-mask:
      linear-gradient(#fff 0 0) padding-box,
      linear-gradient(#fff 0 0);
    -webkit-mask-composite: destination-out;
    mask-composite: exclude;
    opacity: 0;
    transition: opacity 0.3s ease-in-out;
  }

  .project-card:hover::before {
    opacity: 1;
  }

  .gradient-text {
    /* The gradient background */
    background-image: linear-gradient(360deg, #1a2a4c, #5676c9d1, white 40%);

    /* Clip the background to the text */
    -webkit-background-clip: text;
    background-clip: text;

    /* Make the text color transparent */
    -webkit-text-fill-color: transparent;

    /* Ensures the gradient covers the text properly */
    background-size: 100% 100%;
  }
</style>

<div class="min-h-screen text-white p-4 sm:p-6 md:p-8 mt-[100px]">
  <div class="max-w-7xl mx-auto">
    <!-- Header Section -->
    <header class="mb-8 md:mb-12">
      <p class="text-blue-400 font-mono text-sm mb-2">[03] — Our Projects</p>
      <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold mb-4 gradient-text">
        Turing Ideas Into Impactful Solutions!
      </h1>
      <p class="text-xl pt-4 text-gray-300">
        Every project tells a story of innovation, precision, and measurable
        growth.
      </p>
      <!-- <button
        class="bg-green-500/20 text-green-300 border border-green-400/50 rounded-full px-5 py-2 text-sm font-medium flex items-center gap-2 hover:bg-green-500/30 transition-all duration-300 group"
      >
        View my resume
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="16"
          height="16"
          fill="currentColor"
          class="bi bi-arrow-up-right-circle transition-transform duration-300 group-hover:scale-125"
          viewBox="0 0 16 16"
        >
          <path
            fill-rule="evenodd"
            d="M1 8a7 7 0 1 0 14 0A7 7 0 0 0 1 8m15 0A8 8 0 1 1 0 8a8 8 0 0 1 16 0M5.854 10.854a.5.5 0 0 0 .708 0L12 5.707V8.5a.5.5 0 0 0 1 0V4.5a.5.5 0 0 0-.5-.5H8.5a.5.5 0 0 0 0 1H11.293L5.146 10.146a.5.5 0 0 0 0 .708"
          ></path>
        </svg>
      </button> -->
    </header>

    <!-- Grid Layout for Projects -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <!-- Project 1:  (Spans 2 columns) -->
      <div
        class="project-card relative md:col-span-2 p-4 rounded-lg border border-gray-800/50 flex flex-col transition-transform duration-300 hover:scale-[0.98]"
      >
        <a href="/projects/cryptocademy-app" class="z-50">
          <div class="mb-4">
            <p class="text-xs text-gray-400">Mobile App Development</p>
            <h2 class="text-xl font-semibold mt-1">
              Cryptocademy crossed 20k signups on Android!
            </h2>
          </div>
          <div class="flex-grow rounded-md w-full aspect-video">
            <!-- Placeholder for image -->
            <Image src={cryptocademyImg} alt="Cryptocademy" class="" />
          </div>
        </a>
      </div>

      <!-- Project 2: Riding the Wave -->
      <div
        class="project-card relative p-4 rounded-lg border border-gray-800/50 flex flex-col transition-transform duration-300 hover:scale-[0.98]"
      >
        <a href="/projects/me-n-you-app" class="z-50">
          <div class="mb-4">
            <p class="text-xs text-gray-400">Generative AI</p>
            <h2 class="text-xl font-semibold mt-1">
              Webdrip is working on it's local first AI Companion App! Set to
              release on 2025
            </h2>
          </div>
          <div class="flex-grow rounded-md w-full aspect-video">
            <!-- Placeholder for image -->
            <Image
              src={menyouai}
              alt="menyou ai companion teaser"
              class="object-contain"
            />
          </div>
        </a>
      </div>

      <!-- Project 4 -->
      <div
        class="project-card relative p-4 rounded-lg border border-gray-800/50 flex flex-col transition-transform duration-300 hover:scale-[0.98]"
      >
        <a href="/projects/workout-wiki-app" class="z-50">
          <div class="mb-4">
            <p class="text-xs text-gray-400">Mobile App | VISION</p>
            <h2 class="text-xl font-semibold mt-1">
              Workout Wiki - 1200+ Exercises
            </h2>
          </div>
          <div class="flex-grow rounded-md w-full aspect-video">
            <!-- Placeholder for image -->
            <Image
              src={Workoutwiki}
              alt="Workout Wiki App - Webdrip"
              class="object-contain"
            />
          </div>
        </a>
      </div>

      <!-- Project 3: Omelo -->
      <div
        class="project-card relative p-4 rounded-lg border border-gray-800/50 flex flex-col transition-transform duration-300 hover:scale-[0.98]"
      >
        <a href="/projects/signlanguage-app" class="z-50">
          <div class="mb-4">
            <p class="text-xs text-gray-400">Mobile App | VISION</p>
            <h2 class="text-xl font-semibold mt-1">
              SignLanguage: Learn ASL Practically
            </h2>
          </div>
          <div class="flex-grow rounded-md w-full aspect-video">
            <!-- Placeholder for image -->
            <Image
              src={GestureAcademyImg}
              alt="GestureAcademy App"
              class="object-contain"
            />
          </div>
        </a>
      </div>

      <!-- View All Projects Card -->
      <a
        href="/projects"
        class="bg-main text-white p-6 rounded-lg flex items-center justify-center aspect-video md:aspect-auto transition-transform duration-300 hover:scale-[0.98]"
      >
        <div class="flex items-center gap-2 font-semibold text-2xl">
          View all projects
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="20"
            height="20"
            fill="currentColor"
            class="bi bi-arrow-right-circle-fill"
            viewBox="0 0 16 16"
          >
            <path
              d="M8 0a8 8 0 1 1 0 16A8 8 0 0 1 8 0M4.5 7.5a.5.5 0 0 0 0 1h5.793l-2.147 2.146a.5.5 0 0 0 .708.708l3-3a.5.5 0 0 0 0-.708l-3-3a.5.5 0 1 0-.708.708L10.293 7.5z"
            ></path>
          </svg>
        </div>
      </a>
    </div>
  </div>
</div>

<style>
  .bg-main {
    background-image: linear-gradient(180deg, var(--bg-darker), #2e2e2e00 49%),
      radial-gradient(
        circle farthest-corner at 50% 100%,
        #d3d7033d,
        #36363600 63%
      ),
      radial-gradient(circle farthest-corner at 100% 100%, #ffffff03, #0000 39%),
      linear-gradient(180deg, #0000 70%, #ffffff0d),
      radial-gradient(circle farthest-corner at 55% 0%, #0e0e0f, #2e2e2e00),
      linear-gradient(
        180deg,
        var(--bgp-dark),
        #8da2e3db 62%,
        var(--bgp-lighter)
      ),
      url("../../assets/giphy-1.gif");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }
</style>
